{
    "name": "name",
    "scripts": {
        "build": "cd apps/back && npm run build",
        "start": "cd apps/back && npm start"
    },
    "engines": {
        "node": "20.12.2"
    },
    "dependencies": {
        "@types/uuid": "^10.0.0"
    }
}
